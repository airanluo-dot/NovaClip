# FFmpeg

Development builds may use a user-provided `ffmpeg.exe` at `tools/ffmpeg/win-x64/ffmpeg.exe`, an explicit path in Settings, or an executable available on PATH.

Do not commit a third-party FFmpeg binary until its exact version, build flags, source offer and LGPL/GPL obligations are documented.
